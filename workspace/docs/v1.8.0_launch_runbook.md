# v1.8.0 Launch Runbook

> **修订**: 2026-07-04 · **状态**: DRAFT
> **前置**: 24h B 线观察窗结束，Go 决策通过
> **预计耗时**: 15 分钟

---

## Phase 0: 状态确认（1 分钟）

- [ ] 运行 `python scripts/auto_fixer.py` → 输出 `status: ANALYZED`
- [ ] 检查 `data/runs/evolution_status.json` → pass_rate 1.0
- [ ] 检查 `docs/v1.8.0_go_nogo_checklist.md` → 全部通过

## Phase 1: 合入 auto_fixer（5 分钟）

- [ ] 在 `scripts/orchestrator.py` 的 Step 3（rollback）后、Step 4（evolve）前插入：

```python
from scripts.auto_fixer import analyze_failures, select_strategy, apply_repair, should_rollback, log_fix_attempt

eval_data = load_eval()
if eval_data:
    analysis = analyze_failures(eval_data)
    plan = select_strategy(analysis)
    if plan["strategy"] != "NONE":
        pre_rate = eval_data.get("pass_rate", 1.0)
        result_path = apply_repair(plan["strategy"])
        post_rate = run_health_checks()  # 重新评估
        rb = should_rollback(pre_rate, post_rate)
        log_fix_attempt(plan, {"result": "applied" if rb["rollback"] else "failed"})
```

- [ ] 跑一次完整 pipeline → pass_rate ≥ 1.0

## Phase 2: 灰度 P1 启用（3 分钟）

- [ ] 确认 `select_strategy()` 返回 S1 且 confidence ≥ 0.8
- [ ] `apply_repair("S1")` 写入模板
- [ ] 验证 `configs/agent_chain.yaml` 内容正确

## Phase 3: 交付（5 分钟）

- [ ] `git commit -m "feat(auto-fixer): integrate into orchestrator, enable P1"`
- [ ] `git tag v1.8.0-release`
- [ ] 发送上线公告（`docs/v1.8.0_release_announcement.md` 修改后发出）
- [ ] 老板确认，Done.

## 回滚

如果上线后 1 小时内出现任何异常：

```powershell
git revert HEAD --no-edit
git tag v1.8.0-rollback
```
