{
  "name": "code-navigator",
  "version": "0.2.0",
  "entrypoint": "./run.bat",
  "status": "implemented",
  "type": "script",
  "args": [
    {
      "name": "projectDir",
      "type": "string",
      "description": "Path to source tree to search",
      "required": true
    },
    {
      "name": "symbol",
      "type": "string",
      "description": "Symbol name to find (optional — omit to list all)",
      "required": false
    },
    {
      "name": "type",
      "type": "string",
      "description": "Filter by kind: func, class, interface, type, enum, import, export, all",
      "required": false
    },
    {
      "name": "fuzzy",
      "type": "bool",
      "description": "Fuzzy (regex) match instead of exact match",
      "required": false
    },
    {
      "name": "json",
      "type": "bool",
      "description": "Output as JSON (for script use)",
      "required": false
    }
  ],
  "output": {
    "success": "Table of symbols with file:line references, grouped by file",
    "failure": "Exit code 1 if no matches found"
  }
}
