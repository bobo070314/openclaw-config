---
name: web-deploy-github
description: Deploy static sites and web apps to GitHub Pages. Handles build, asset optimization, and deployment with a single command.
version: 0.1.0
category: devops
---

# web-deploy-github

Deploy static sites and web apps to GitHub Pages. Handles build, asset optimization, and deployment with a single command.

## Usage

```bash
python web-deploy-github/run.py --help
python web-deploy-github/run.py --json
python web-deploy-github/run.py --dry-run
```

## Features

- Feature 1 (TBD)
- Feature 2 (TBD)

## Requirements

- Python 3.11+
- GitHub CLI (`gh`) or GitHub API token

## Status

v0.1.0 — MVP scaffold. Ready for API integration.
